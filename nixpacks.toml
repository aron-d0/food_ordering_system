[phases.setup]
nixPkgs = ["php84", "nodejs"]

[start]
cmd = "mkdir -p database && touch database/database.sqlite && php artisan migrate --force --seed && php artisan serve --host=0.0.0.0 --port=$PORT"
